apiVersion: v1
kind: Service
metadata:
  name: mojo-strategy-liquidation-app
spec:
  selector:
    app: mojo-strategy-liquidation-app
  ports:
  - port: 9994
    targetPort: 9994
    nodePort: 31447
  type: NodePort
